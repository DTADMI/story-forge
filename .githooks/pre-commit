#!/usr/bin/env bash
set -euo pipefail

echo "==> StoryForge pre-commit checks"

echo "-> Running tests..."
pnpm -C web test:run 2>&1 || echo "[WARN] Tests failed — review before pushing"

echo "-> Pre-commit checks complete"

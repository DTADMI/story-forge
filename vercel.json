{
  "buildCommand": "pnpm -C web build",
  "outputDirectory": "web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_SITE_URL": "https://storyforge.app"
  }
}
